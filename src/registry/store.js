export default ({}) => {
  // Constants
  const state = {
    // Layout Setting
    layout: {
      // Sider Control
      sider: true,

      // Toper Control
      toper: true,
    },
  };

  const mutations = {};

  const actions = {};

  return { state, mutations, actions };
};
